{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://meshsat.net/schemas/meshsat-android/bridge-trust-entry.json",
  "title": "BridgeTrustEntry",
  "description": "One row of the bridge_trust Room table (introduced in MIGRATION_12_13). Persisted in data/BridgeTrustEntity.kt; this schema serialises the row for export / debugging / cross-process IPC.",
  "type": "object",
  "required": ["bridge_hash", "pubkey", "first_seen_at", "last_seen_at"],
  "additionalProperties": false,
  "properties": {
    "bridge_hash": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "SHA-256 hash of the bridge's stable identifier."
    },
    "pubkey": {
      "type": "string",
      "pattern": "^[A-Za-z0-9+/=]{1,128}$",
      "description": "Base64-encoded Ed25519 public key."
    },
    "first_seen_at": {
      "type": "integer",
      "minimum": 0,
      "description": "Unix epoch milliseconds when this bridge was first imported."
    },
    "last_seen_at": {
      "type": "integer",
      "minimum": 0,
      "description": "Unix epoch milliseconds of the most recent import for this bridge_hash."
    },
    "alias": {
      "type": ["string", "null"],
      "description": "Operator-friendly nickname; nullable."
    }
  }
}
