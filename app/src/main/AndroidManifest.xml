<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet for map tiles -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- SMS: only the fdroid edition. Its permissions and receivers are in
         src/fdroid/AndroidManifest.xml; the play edition has none of them (MESHSAT-1335). -->

    <!-- Bluetooth (Meshtastic BLE + HC-05 SPP) -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <!-- Foreground service for long-running gateway (BLE + location + messaging) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_CONNECTED_DEVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_LOCATION" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_REMOTE_MESSAGING" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <uses-feature android:name="android.hardware.bluetooth_le" android:required="false" />
    <uses-feature android:name="android.hardware.bluetooth" android:required="false" />
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <!-- So the emergency-contact picker can find the phone's own contacts app and go straight to
         it, instead of Android asking which of several apps should "pick" (MESHSAT-1291). This
         lets the app SEE which apps answer that one intent; it reads no contacts. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PICK" />
            <data android:mimeType="vnd.android.cursor.dir/phone_v2" />
        </intent>
    </queries>

    <application
        android:name=".MeshSatApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="MeshSat"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.MeshSat">

        <!-- The QR scanner (Hub code, key sync, contact cards), upright and with the status bar.
             The library's own screen is locked to landscape and full screen, so the phone had to
             be turned sideways to scan a code (owner, 21 Sep 2026, MESHSAT-1299). -->
        <activity
            android:name="com.journeyapps.barcodescanner.CaptureActivity"
            android:screenOrientation="portrait"
            android:theme="@android:style/Theme.Material.NoActionBar"
            tools:replace="android:screenOrientation,android:theme" />

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Deep link: meshsat://provision/* for Hub QR provisioning -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="meshsat" android:host="provision" />
            </intent-filter>
        </activity>

        <!-- Start after a phone restart or an app update, when the user chose it (MESHSAT-1249) -->
        <receiver
            android:name=".service.BootReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
            </intent-filter>
        </receiver>

        <!-- Gateway foreground service -->
        <service
            android:name=".service.GatewayService"
            android:exported="false"
            android:foregroundServiceType="connectedDevice|location|remoteMessaging" />

    </application>
</manifest>
