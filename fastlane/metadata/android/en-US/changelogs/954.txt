The rule editor now says so when a rule would send the Hub a second copy of a satellite message: the Hub already gets those straight from the satellite provider, so a rule like that makes everything at the far end happen twice.
